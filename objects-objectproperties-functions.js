//practice
const pizza = {
  toppings: ["cheese", "sauce", "pepperoni"],
  crust: "deep dish",
  serves: 2,
};

console.log(pizza);

//object keys
const car = {
  make: "Honda",
  model: "Accord",
  year: 2020,
};

const keys = Object.keys(car);

console.log(keys);
